% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ss_time_dependent_roc.R
\name{ss_time_dependent_roc}
\alias{ss_time_dependent_roc}
\title{Sample Size for Time-Dependent ROC Analysis}
\usage{
ss_time_dependent_roc(
  mu_case = 4.5,
  mu_control = 2.5,
  sigma = 2.5,
  lambda_event = 0.25,
  t_horizon = 2,
  censoring_rates = c(0.1, 0.2, 0.3),
  delta_auc = 0.06,
  target_prob = 0.8,
  N_range = seq(100, 500, by = 20),
  B = 500,
  seed = 2026
)
}
\arguments{
\item{mu_case}{Mean biomarker value in cases. Default 4.5.}

\item{mu_control}{Mean biomarker value in controls. Default 2.5.}

\item{sigma}{SD of biomarker (both groups). Default 2.5.}

\item{lambda_event}{Event rate per year (exponential). Default 0.25.}

\item{t_horizon}{Time point for AUC(t) evaluation in years. Default 2.}

\item{censoring_rates}{Numeric vector of censoring proportions.
Default \code{c(0.10, 0.20, 0.30)}.}

\item{delta_auc}{Target precision (half-width) for AUC(t). Default 0.06.}

\item{target_prob}{Probability of achieving precision. Default 0.80.}

\item{N_range}{Range of N to search. Default \code{seq(100, 500, by = 20)}.}

\item{B}{MC replications. Default 500 (lower due to timeROC cost).}

\item{seed}{Random seed. Default 2026.}
}
\value{
Object of class \code{"dtasamplesize"} with additional elements:
  \describe{
    \item{results}{Data frame with columns \code{censoring_rate},
      \code{N_required}, \code{prob_achieved}.}
  }
}
\description{
Monte Carlo-based sample size estimation for achieving a target
precision of the area under the time-dependent ROC curve, AUC(t),
at a given time horizon. Uses the \pkg{timeROC} package for
AUC(t) estimation with inverse-probability-of-censoring weighting.
}
\note{
Parameters in the default example are HYPOTHETICAL. No published
  AUC values exist for ctDNA as a continuous discriminator in DLBCL.
  Requires the \pkg{timeROC} package (listed in Suggests).
}
\examples{
\donttest{
result <- ss_time_dependent_roc(B = 50, N_range = seq(100, 300, by = 50))
print(result)
}
}
