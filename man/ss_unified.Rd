% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ss_unified.R
\name{ss_unified}
\alias{ss_unified}
\title{Unified Monte Carlo Framework for DTA Sample Size}
\usage{
ss_unified(
  prior_se = c(17, 3),
  prior_sp = c(2, 2),
  prior_prev = c(5, 15),
  Se_ref = 0.92,
  Sp_ref = 0.95,
  loss_rate = 0.1,
  delta_se = 0.07,
  delta_sp = 0.05,
  delta_auc = 0.06,
  check_nb = FALSE,
  pt_range = c(0.15, 0.4),
  target_assurance = 0.8,
  N_range = seq(200, 1000, by = 20),
  B = 5000,
  seed = 2026
)
}
\arguments{
\item{prior_se}{Beta prior for Se: \code{c(alpha, beta)}.
Default \code{c(17, 3)}.}

\item{prior_sp}{Beta prior for Sp: \code{c(alpha, beta)}.
Default \code{c(2, 2)}.}

\item{prior_prev}{Beta prior for prevalence: \code{c(alpha, beta)}.
Default \code{c(5, 15)}.}

\item{Se_ref}{Reference standard sensitivity. Default 0.92.}

\item{Sp_ref}{Reference standard specificity. Default 0.95.}

\item{loss_rate}{Expected losses. Default 0.10.}

\item{delta_se}{Half-width target for Se. Default 0.07.}

\item{delta_sp}{Half-width target for Sp. Default 0.05.}

\item{delta_auc}{Half-width target for AUC. Default 0.06.
Set to 0 to skip AUC check.}

\item{check_nb}{Logical, check Net Benefit > 0. Default \code{FALSE}.}

\item{pt_range}{Threshold range for NB if \code{check_nb}.
Default \code{c(0.15, 0.40)}.}

\item{target_assurance}{Joint assurance target. Default 0.80.}

\item{N_range}{Search range for total N. Default \code{seq(200, 800, by = 20)}.}

\item{B}{MC replications. Default 5000.}

\item{seed}{Random seed. Default 2026.}
}
\value{
Object of class \code{"dtasamplesize"} with additional elements:
  \describe{
    \item{joint_assurance}{Achieved joint assurance at optimal N.}
    \item{comparison}{Data frame comparing methods.}
  }
}
\description{
Integrates parameter uncertainty, imperfect reference standards,
and multiple accuracy metrics into a single sample size calculation.
Simulates the full data-generating process including prevalence
uncertainty, reference standard misclassification, and optionally
AUC and net benefit constraints.
}
