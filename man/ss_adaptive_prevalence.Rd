% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ss_adaptive_prevalence.R
\name{ss_adaptive_prevalence}
\alias{ss_adaptive_prevalence}
\title{Adaptive Sample Size with Blinded Prevalence Re-estimation}
\usage{
ss_adaptive_prevalence(
  Se = 0.85,
  Sp = 0.9,
  d_se = 0.07,
  d_sp = 0.05,
  prev_initial = 0.3,
  prev_true_range = c(0.18, 0.25, 0.3, 0.35, 0.42),
  fraction_stage1 = 0.4,
  loss_rate = 0.1,
  B = 5000,
  seed = 2026
)
}
\arguments{
\item{Se}{Expected sensitivity. Default 0.85.}

\item{Sp}{Expected specificity. Default 0.90.}

\item{d_se}{Precision for Se (half-width). Default 0.07.}

\item{d_sp}{Precision for Sp (half-width). Default 0.05.}

\item{prev_initial}{Initially assumed prevalence. Default 0.30.}

\item{prev_true_range}{Numeric vector of true prevalence scenarios.
Default \code{c(0.18, 0.25, 0.30, 0.35, 0.42)}.}

\item{fraction_stage1}{Fraction of initial N for stage 1. Default 0.40.}

\item{loss_rate}{Expected loss-to-follow-up rate. Default 0.10.}

\item{B}{MC replications. Default 5000.}

\item{seed}{Random seed. Default 2026.}
}
\value{
Object of class \code{"dtasamplesize"} with additional elements:
  \describe{
    \item{N_initial}{Initial planned N (before adaptation).}
    \item{n_stage1}{Number of subjects in stage 1.}
    \item{results}{Data frame with one row per true prevalence scenario,
      showing N_final mean/median/P75, precision achieved.}
  }
}
\description{
Simulates a two-stage adaptive design where prevalence is estimated
at an interim stage and the sample size is adjusted upward if the
observed prevalence is lower than initially assumed.
}
\details{
The parameters \code{d_se} and \code{d_sp} are
  \strong{half-widths} of the confidence interval. The full CI width
  target is \code{2 * d}.
}
\examples{
result <- ss_adaptive_prevalence(B = 500, prev_true_range = c(0.20, 0.30))
print(result)
}
\references{
Stark M, Zapf A (2020). Sample size calculation and re-estimation for
diagnostic accuracy studies based on sensitivity and specificity.
\emph{Stat Methods Med Res} 29:2958-2971.
\doi{10.1177/0962280220913588}
}
